version: "3.8"
services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: altaa_test
    ports:
      - "5432:5432"
    volumes:
      - db-data:/var/lib/postgresql/data

  # api:
  #   build:
  #     context: ./
  #     dockerfile: Dockerfile
  #   environment:
  #     DATABASE_URL: "postgresql://postgres:postgres@db:5432/altaa_test?schema=public"
  #     JWT_SECRET: "replace_in_prod"
  #   ports:
  #     - "4000:4000"
  #   depends_on:
  #     - db

  # frontend:
  #   build:
  #     context: ../frontend
  #     dockerfile: Dockerfile
  #   environment:
  #     NEXT_PUBLIC_API_URL: "http://localhost:4000"
  #   ports:
  #     - "3000:3000"
  #   volumes:
  #     - ../frontend:/app
  #     - /app/node_modules
  #   depends_on:
  #     - api

volumes:
  db-data:
